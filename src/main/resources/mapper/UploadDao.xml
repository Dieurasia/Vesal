<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thoughtWorks.dao.UploadDao">

    <insert id="addZipInfo" parameterType="map">
        insert into v_model
        (m_code, m_name, m_price, m_introduce, m_thumbnail, m_dynamic, m_unity, m_file)
        values
        (#{zipFile.code},#{zipFile.name},#{zipFile.price},#{zipFile.introduce},#{zipFile.imgStr},#{zipFile.gifStr},#{zipFile.gifStr},#{zipFile.gifStr});
    </insert>

</mapper>